HOST=127.0.0.1
PORT=3100
MAX_CONCURRENT_RUNS=4
# Initial seed only; afterwards manage URL/name through the sidebar (MongoDB).
MODEL_BASE_URL=http://192.168.8.231:8200/v1
MODEL_NAME=qwen38-27b
# MODEL_API_KEY=
MODEL_TIMEOUT_MS=900000
DATASET_PATH=math_qa_275_20260617.mineru.filled_reference_answer-v2.jsonl
RUNS_DIR=data/runs
# RUNS_DIR is only the source directory for importing legacy JSON history.
IMAGES_DIR=images
MONGODB_HOST=127.0.0.1
MONGODB_PORT=27017
MONGODB_DATABASE=qwen_math_lab
MONGODB_USERNAME=qwen_math_lab
MONGODB_PASSWORD=replace_with_your_password
# Defaults to MONGODB_DATABASE; use admin only if the user was created there.
# MONGODB_AUTH_SOURCE=qwen_math_lab
# Optional connection string (takes precedence over host/port/user/password):
# MONGODB_URI=mongodb://...
