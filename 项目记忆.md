# 项目记忆

## bjb1-892：GPU 挂载策略

- bjb1-892 是按 GPU 使用计费的云实例。
- 为避免空闲计费，在不运行推理任务时可以不挂载 GPU。
- 因此通过 SSH 登录后，`nvidia-smi` 看不到 GPU、`/dev/nvidia*` 不存在，或 `nvidia-smi` 不可用，均可能是**预期的空闲状态**，不应直接判断为实例或驱动故障。
- 仅在准备安装、验证 CUDA 或启动 vLLM 前，才需要在云平台挂载/启用 RTX PRO 6000；任务结束后可再次卸载以停止 GPU 计费。
